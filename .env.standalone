# SimplyInspect Standalone Environment Configuration

# Azure/Microsoft Configuration (REQUIRED - Replace with your values)
AZURE_TENANT_ID=your_tenant_id_here
AZURE_CLIENT_ID=your_client_id_here
AZURE_CLIENT_SECRET=your_client_secret_here
SHAREPOINT_URL=https://your-tenant.sharepoint.com

# Database Configuration (Default values for standalone deployment)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=simplyinspect
DB_USER=simplyinspect
DB_PASSWORD=SimplyInspect2024!

# Application Ports (External ports)
API_PORT=8001
UI_PORT=3001
DB_EXTERNAL_PORT=5433